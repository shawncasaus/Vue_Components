export enum HealthColors {
  GREEN = '#60cd8e',
  YELLOW = '#ffc340',
  RED = '#e40139'
}

export enum HealthThresholds {
  HIGH = 'High',
  MEDIUM = 'Medium',
  LOW = 'Low',
  NED = 'Not Enough Data'
}

export enum HealthScoreTypeNames {
  ONLINE = 'Online',
  FIRST_MONTH_EXIT = '1st Month Exit',
  RETENTION = 'Retention',
  EXIT = 'Exit',
  ENGAGEMENT = 'Engagement'
}
